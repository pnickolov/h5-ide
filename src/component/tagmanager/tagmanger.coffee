define [
    'UI.modalplus'
    'constant'
    'ApiRequest'
], ( modalplus, constant, ApiRequest ) ->

    Backbone.View.extend

        className: 'tag-manager'

        tagName: 'section'

        initialize: (option) ->

        events:

        render: () ->
