#############################
#  View Mode for reset
#############################

define [ 'MC', 'event' ], ( MC, ide_event ) ->

    #private
    ReSetModel = Backbone.Model.extend {

        #defaults   :

        initialize : ->
            #

    }

    return new ReSetModel()