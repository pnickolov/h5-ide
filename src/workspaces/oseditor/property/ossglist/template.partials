<!-- {{stack}} -->
<div class="option-group-head expand">
    Security Group
</div>
<section class="group">
    <label class="name">Security Group</label>
    <select class="selection option mutil item-list" value="nop,abc,def" data-button-tpl="button" data-item-tpl="sgItem">
        {{#each sgListData}}
        <option value="{{uid}}">{{name}}</option>
        {{/each}}
    </select>
</section>

<!-- {{sgitem}} -->
<div class="item">
    <span class="item-name">{{name}}</span>
    <div class="item-remove icon-delete"></div>
</div>

<!-- {{addSGButton}} -->
<div>Create New Security Group...</div>
