<!-- {{stack}} -->
<article>
    <div class="option-group-head expand">Dependency relationship</div>
    <div class="option-group" data-bind="true">
        <label>{{after}} depends on {{before}}</label>
    </div>
</article>
