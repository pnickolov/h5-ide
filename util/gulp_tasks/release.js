(function() {
  var build, gulp, include;

  gulp = require("gulp");

  include = require("./plugins/include");

  build = function(debugMode) {
    return null;
  };

  module.exports = {
    build: build
  };

}).call(this);
